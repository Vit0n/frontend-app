import { Layout } from '../components/Layout.tsx';

export const AboutPage = () => {
    return (
        <Layout>
            <h2>About</h2>

            <p>This is a simple example of a React application with TypeScript.</p>
        </Layout>
    )
};
